import type {SidebarsConfig} from '@docusaurus/plugin-content-docs';

const sidebars: SidebarsConfig = {
  // 文档侧边栏：包含 docs 根目录下的文档，排除 api 子目录
  tutorialSidebar: [
    'intro',
    'quick-start',
    'markdown-syntax',
    'theme-customization',
    'deployment',
    'changelog',
  ],

  // API 侧边栏
  apiSidebar: [
    {type: 'autogenerated', dirName: 'api'},
  ],

  // 知识侧边栏：docs/konwledge 目录下的文章
  knowledgeSidebar: [
    {type: 'autogenerated', dirName: 'konwledge'},
  ],
};

export default sidebars;
